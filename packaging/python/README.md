# sherpa-code

This platform wheel installs the native Sherpa codebase-intelligence CLI and editor server.

```sh
pipx install sherpa-code
sherpa --version
```

The distribution name is `sherpa-code` because the `sherpa` name on PyPI belongs to an unrelated
astronomy project. This package provides executables, not a Python programming API.
